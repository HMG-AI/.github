name: Mutable job container
on: push
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    container: ruby:3.4
    services:
      cache:
        image: redis:7
    steps:
      - run: "true"
